{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": [".next/**", "dist/**"] },
    "dev": { "cache": false },
    "lint": {},
    "test": {},
    "test:run": {},
    "typecheck": { "dependsOn": ["^build"], "cache": false },
    "compile-check": { "cache": false }
  }
}
